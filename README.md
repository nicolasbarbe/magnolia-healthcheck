# Magnolia Healthcheck
 A simple servlet returning an HTTP response 200 and 'OK' in the body if Magnolia is up and running. The servlet is mounted at this address : ./healthcheck/ (don't forget the last slash)

